# Optional. Without it the app still works — it falls back to OpenStreetMap
# venue data only, and the Ticketmaster source reports itself as "skipped".
#
# Get a free key at https://developer.ticketmaster.com/
VITE_TICKETMASTER_API_KEY=
